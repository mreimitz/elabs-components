# Orchestrator prompt — chart-definitions track

You are the orchestrator for `roadmap/chart-definitions/` (RM-162 … RM-205) in `mreimitz/elabs-components`. Read `roadmap/README.md`, this folder's `README.md`, ADR 0042 (`docs/ADR/0042-chart-definitions-and-prop-groups.md`), the source review `docs/review/2026-09-25-charts-unification-review.md`, `docs/DEPRECATION.md`, `.claude/rules/conventions.md` and `.claude/rules/charts.md` before anything else.

Protocol:

1. RM-162 first. ADR 0042 records the maintainer's three decisions as accepted; its Appendix A is the **frozen rename table**. Waves 1 and A depend on neither RM-162 nor the rename table. RM-191 … RM-196 do not start until the maintainer has reviewed Appendix A (confirmation item (d)), and no rename item adds a name the table does not list — a change to the table is an ADR amendment.
2. Items are tracked in this folder only (front matter + table status); no GitHub issues. A subagent (agent / model from the item's front matter) gets the item file verbatim plus ADR 0042, works in a git worktree on a `charts/rm-NNN-<slug>` branch cut from `charts/unification`, and touches only the paths in `touches`. If it must edit a file outside them, it stops and reports instead.
3. Waves as drawn in the README; items in a wave run in parallel only when their `touches` are disjoint. Wave 1 and wave A run in parallel. Wave 2 needs only wave A, but RM-173 edits files wave 1 also edits (`chart-config-context.tsx` with RM-167, `legend/use-container-legend.ts` with RM-163) — merge the wave-1 item first and rebase. In wave 3, `time-series-chart-shell.tsx` belongs to RM-182, and RM-185 rebases after RM-182 for `waterfall-chart.tsx`. Shared files are append-only under a `// <Name> — RM-NNN` comment: `packages/charts/src/charts/index.ts`, `packages/charts/src/definitions/registry.ts`, `packages/ui/src/components/locale-provider/messages.ts`; run `pnpm check:update` deliberately after a baseline change, never blind.
4. An item is done only on: gates green (`pnpm check:changed`, `pnpm --filter <pkg> typecheck lint test` for every touched package, `pnpm check --rule <ids in the item>`, `pnpm gen && pnpm gen:check` twice), its Acceptance met with quoted evidence, and a **browser proof** — `pnpm test:stories` (the CLI runner; the MCP runner serves stale package code) on the touched stories in Chromium, light and dark, 380 / 600 / 900 px, with the keyboard path exercised in a play function where the item is interactive. Unit-test-only evidence is not done.
5. Defaults move verbatim. Every adoption PR carries the defaults-parity test; visual baselines move only under review; the manifest diff shows additions only. The RM-146 restatement blocks are deleted (RM-180) only after the regenerated prop diff (RM-179) is reviewed.
6. Every rename follows `docs/DEPRECATION.md` in full — `@deprecated` TSDoc naming the replacement, an autodocs note, a `### Deprecated` changeset bullet, an alias row, `warnOnce` in dev — and migrates internal callers in the same PR, so consumers never see a warning for code they did not write. `charts-deprecated-usage` stays at zero findings.
7. After each wave: merge into `charts/unification`, run the full gates once (`pnpm check`, `pnpm check:test`, `pnpm -r typecheck lint test`, `pnpm build`, `node packages/cli/scripts/check-chart-treeshake.mjs` once RM-181 has landed, `pnpm consumer:check`, `pnpm test:stories`), run the review lane (`brand-ui-reviewer`), then start the next wave. Each wave's changesets follow the README's semver column; versioning is lockstep.
8. Neutral wording in every file: never name the reference product or its chart framework — the `reference-leakage` rule and the plan-references hook block them. Say "the reference chart templates" if a reference is needed.
9. When RM-170 and RM-171 merge, tell the flow track that its Phase 1 may start and point it at the README's "Flow coordination" section.
10. The agent flips `status` in its own RM file; the orchestrator updates this folder's table at merge.
11. RM-205 (6.0.0) starts only when the maintainer says release, at least one full minor after RM-196. Close with the review's `## Outcome`, `roadmap/README.md`'s track row, and one report at the end — merged items, gate results, browser evidence, and anything that could not stay inside its `touches`.
