---
id: RM-188
title: "Reference marks and axes: one reference painter, one trend painter, `chart-style-constants`, shared axes, one zoom-control component"
status: done
priority: P1
effort: L (4 days)
wave: 3
depends_on: [RM-182, RM-183, RM-184, RM-185]
blocks: [RM-192, RM-195, RM-201]
agent: brand-ui-component-builder
model: opus
touches:
  - packages/charts/src/charts/reference-line.tsx, trend-line.tsx, bar-overlays.tsx, pie-chart.tsx, dumbbell-chart.tsx (painters)
  - packages/charts/src/charts/annotations/chart-annotations.tsx, packages/charts/src/charts/analytics/derived-series.ts, packages/charts/src/charts/analytics/analytic-series-layer.tsx, packages/charts/src/charts/distribution/distribution-reference-line.tsx (painters)
  - packages/charts/src/charts/chart-stroke.ts, chart-opacity.ts (the one dash map and the dim constants)
  - scripts/check/rules/chart-style-constants.mjs (new — count baseline for dash, opacity and duration literals)
  - packages/charts/src/charts/heatmap/heatmap-chart.tsx, packages/charts/src/charts/distribution/distribution-value-axis.tsx, packages/charts/src/charts/density-scatter/density-scatter-chart.tsx, packages/charts/src/charts/parallel-coordinates/parallel-coordinates-chart.tsx (hand-rolled axes onto shared primitives)
  - packages/charts/src/charts/y-axis-ticks.ts, tick-targets.ts (one tick table)
  - packages/charts/src/charts/bar-x-axis.tsx, bar-y-axis.tsx, x-axis.tsx, axis-title.tsx (titles on the bar axes; one crosshair-label fade helper; the raw `--color-zinc-500` fallback goes)
  - packages/charts/src/charts/gestures/chart-zoom-controls.tsx, packages/charts/src/charts/choropleth/zoom-controls.tsx, packages/charts/src/charts/tree-chart-viewport.tsx, packages/charts/src/gantt/gantt.tsx (one zoom-button component)
  - packages/charts/src/charts/scatter-chart.tsx, candlestick-chart.tsx, dumbbell-chart.tsx, distribution/distribution-chart.tsx (`annotations` typed; `ifOverflow: "extend"` wired)
  - scripts/check/baseline.json (`chart-style-constants` count)
  - docs/GATES.md (regenerated by `pnpm gen`)
  - .changeset/*.md (minor — bar-axis titles; one zoom-control component)
source: docs/review/2026-09-25-charts-unification-review.md F19, F20, F21, F24, F32; ADR 0042 (reference-marks and axis groups)
---

# RM-188 Reference marks and axes: one reference painter, one trend painter, `chart-style-constants`, shared axes, one zoom-control component

## Finding

- The reference-line concept has seven painters (ReferenceLine, ChartLineAnnotation, AnalyticLine, Distribution `referenceLines`, Dumbbell `referenceLine`, Bar overlays, Pie `referenceRings`) and trends three (TrendLine, Scatter `trend`, AnalyticTrend), with 10+ dash literals (F19). `.claude/rules/charts.md` says "never a new painter per statistic".
- Scatter and Candlestick accept `annotations` at runtime but do not declare it; `ifOverflow: "extend"` is not wired in Candlestick, Dumbbell or Distribution (F20).
- Four families hand-roll axes (Heatmap, Distribution, DensityScatter, Parallel); BarXAxis and BarYAxis have no title; two default tick tables; the crosshair label fade is copied twice (F24).
- Four zoom-button implementations: ChartZoomControls, ChoroplethZoomControls, TreeChartZoomControls and Gantt's toolbar group (F21).
- Dim and emphasis opacities are per-family literals from 0.035 to 0.6 (F32).

## Change

- One reference painter and one trend painter; the others become thin mappings onto them. One dash map, seeded from `LINE_DASH` (`chart-annotations.tsx:83-87`), in `chart-stroke.ts`; the dim constants in `chart-opacity.ts`.
- `chart-style-constants` counts dash, opacity and duration literals outside the shared constants; the baseline only goes down.
- The four hand-rolled axes move onto the shared axis primitives; one tick table; BarXAxis and BarYAxis gain `title` / `titlePlacement` through `axis-title.tsx`; one fade helper.
- The four zoom-button implementations become one component, honouring `useChartInteractionPolicy`.
- Scatter and Candlestick type `annotations`; Candlestick, Dumbbell and Distribution wire `useAnalyticsExtents`.

## Acceptance

- `chart-style-constants` ratchets down (numbers in the PR).
- Baselines reviewed; changed pixels are named.
- Keyboard and pointer zoom behave the same on Choropleth, Tree, Gantt and the navigator families (play tests).

## Test / gate

`pnpm --filter @elabs-ai/components-charts typecheck lint test`, `pnpm check:test`, `pnpm check --rule chart-style-constants,chart-hairline,charts-honesty`, `pnpm test:stories` on the touched families, keyboard path exercised, Chromium light and dark at 380 / 600 / 900 px.

## Open follow-ups

- The analytic `trend` still draws through the derived-series `LinePath` (`analytic-series-layer.tsx`), not `TrendRule`: it is a multi-point path with the derived-series label, legend and tooltip wiring, not a two-point rule.
- `live-x-axis.tsx` keeps its own copy of the crosshair label fade (`labelFadeOpacity`); it should call `crosshairLabelOpacity` from `axis-title.tsx` like `XAxis` and `BarXAxis` now do. The file was outside this item's `touches`.
- Bar overlays (`bar-overlays.tsx`) have no rule to map: they are per-bar markers (a tick or dot) and per-bar spans, not a line across the plot, so there is no reference rule to draw them through. Left unchanged.
- Heatmap's axes stay hand-rolled: they are categorical SVG `<text>` labels with a stride rule, while the shared category axis paints HTML labels with a fit cascade — moving them is a pixel change.
- DensityScatter keeps its own nice-step tick generator and y target: its step thresholds (1.5 / 3.5 / 7.5) differ from d3's (√2 / √10 / √50), so the shared generator would change tick values. Only the x target moved onto the tick table.
- From review round 1:
  - `ChoroplethZoomLabels` has no `group` key, so a consumer who translates Choropleth's button names by passing an object to `zoomControls` cannot translate the group's "Chart zoom" name the same way (it still comes from the chart messages).
  - `ChartZoomControls`' `appearance` is a visual variant that also switches behaviour: `aria-disabled` + handler guard (`segmented`) vs native `disabled` (`overlay`, `toolbar`), the button order, and the root element (`ButtonGroup` for `toolbar`). Split the behaviour out of the visual axis.
  - The hosts replace the base root `data-slot` (`tree-chart-viewport-controls`, `choropleth-zoom-controls`) instead of keeping `chart-zoom-controls` on the root and adding their own part slot.
  - `DistributionChart` resolves its analytics twice: once for the domain extents in the container, once in the annotation host.
  - `BarXAxis` / `BarYAxis` `titlePlacement="inside"` hangs in a fixed plot corner and paints over the last column (vertical) or the first row (horizontal), legible on its halo; stepping around the bar would need mark-aware placement.
