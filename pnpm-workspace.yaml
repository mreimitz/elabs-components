packages:
  - "apps/*"
  - "packages/*"
  # Not a shipped package — see registry/package.json. Copy-own block source that
  # Storybook renders in place, so it needs its own node_modules to resolve.
  - "registry"
