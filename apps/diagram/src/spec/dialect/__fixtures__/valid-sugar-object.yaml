# yaml-language-server: $schema=../../../../schema/arch-diagram.v0.schema.json
# same AST as valid-sugar-shorthand.yaml (paths and form aside)
diagram: "0"
nodes:
  - id: a
  - id: b
flows:
  - { from: a, to: b, label: Nightly export }
  - { from: a, to: b, direction: back, kind: access, secure: sso }
# expect: none
