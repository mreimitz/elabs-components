# yaml-language-server: $schema=../../../../schema/arch-diagram.v0.schema.json
# same AST as valid-nesting-children.yaml (paths aside)
diagram: "0"
zones:
  - id: vpc
    kind: vnet
  - id: subnet
    kind: subnet
    parent: vpc
nodes:
  - id: api
    title: API
    parent: subnet
# expect: missing-owner @ 5:9
