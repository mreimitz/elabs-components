# yaml-language-server: $schema=../../schema/arch-diagram.v0.schema.json
diagram: "0"
title: Lakehouse on AWS with Databricks and Snowflake
direction: LR
nodeStyle: icon
legend: auto

zones:
  - id: aws
    kind: cloud-account
    owner: customer
    provider: aws
    title: AWS account
    children:
      - id: vpc
        kind: vnet
        title: VPC 10.0.0.0/16
        children:
          - id: private
            kind: subnet
            title: Private subnet
            children:
              - id: postgres-rds
                type: datastore
                icon: aws/rds
                title: Postgres (RDS)
                subtitle: OLTP source
              - id: msk
                type: queue
                icon: aws/managed-streaming-for-apache-kafka
                title: Amazon MSK
                subtitle: Kafka
              - id: dbx-jobs
                icon: databricks/databricks
                title: Databricks jobs
                subtitle: Classic compute
          - id: nat
            icon: aws/virtual-private-cloud
            title: NAT gateway
            subtitle: Egress
      - id: s3-landing
        type: datastore
        icon: aws/simple-storage-service
        title: S3 — landing
        subtitle: Object storage
      - id: glue
        icon: aws/glue
        title: AWS Glue
        subtitle: ETL
      - id: s3-curated
        type: datastore
        icon: aws/simple-storage-service
        title: S3 — curated
        subtitle: Object storage
      - id: iam
        icon: aws/identity-and-access-management
        title: IAM
        subtitle: Identity
  - id: databricks
    kind: cloud-account
    owner: saas
    provider: databricks
    title: Databricks (SaaS)
    children:
      - id: dbx-workspace
        icon: databricks/workspace
        title: Databricks workspace
        subtitle: Control plane
  - id: snowflake
    kind: cloud-account
    owner: saas
    provider: snowflake
    title: Snowflake (SaaS)
    children:
      - id: snow-db
        type: datastore
        icon: snowflake/snowflake
        title: Snowflake DB
        subtitle: Storage
      - id: snow-wh
        icon: snowflake/warehouse
        title: Snowflake warehouse
        subtitle: Compute

nodes:
  - id: okta
    type: external
    icon: lucide/key
    title: Okta
    subtitle: SSO
  - id: qlik-cloud
    icon: qlik/cloud
    title: Qlik Cloud
    subtitle: SaaS
  - id: salesforce
    type: external
    icon: salesforce/salesforce
    title: Salesforce
    subtitle: SaaS
    tone: info

flows:
  - salesforce -> s3-landing: AppFlow export
  - postgres-rds -> msk: CDC
  - msk -> s3-landing: Stream
  - s3-landing -> glue: Catalog
  - glue -> s3-curated: ETL
  # Written reversed: the jobs read curated S3, but they sit in the VPC with the sources,
  # so the forward edge made a VPC -> S3 -> VPC cycle and ELK flipped the Catalog step instead.
  - dbx-jobs <- s3-curated: Load
  - dbx-workspace -> dbx-jobs: { label: Schedule and run, kind: control }
  - dbx-jobs -> snow-db: Write
  - snow-db -> snow-wh: Query
  - snow-wh -> qlik-cloud: Direct Query
  - okta -> qlik-cloud: { label: SSO, kind: access, secure: sso }
  - iam -> glue: { label: IAM role, kind: access }
  - nat -> dbx-workspace: { label: Egress, kind: network }
  - vpc -> snowflake: { label: PrivateLink, kind: network, secure: private-link }
