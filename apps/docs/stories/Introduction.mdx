import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as DashboardTemplate from "../../../packages/charts/src/templates-dashboard.stories";
import * as AiAssistantTemplate from "../../../packages/ai/src/templates-ai-assistant.stories";

<Meta title="Docs/Introduction" />

# brand-ui

A **source-owned, token-driven** React component system for building real
applications — dashboards, data grids, AI chat clients, node canvases, maps, code
editors, terminals and the marketing page in front of them. Most component
libraries give you buttons and inputs, then leave you to invent the hard parts.
brand-ui ships the hard parts, all rendered through **one semantic token
system**, so re-branding the entire surface is a stylesheet change — never a
component change. The default look is modern enterprise SaaS — restrained,
app-first, and themeable to any brand.

It is also built to be **read by coding agents**: a CLI, a generated manifest and
a hosted MCP server expose the real props, intent and tokens, so an assistant
extends the system from ground truth instead of guessing.

## Four ways in

- **Build an app now:** `npx -y @elabs-ai/components-cli create my-app --template dashboard`
  — a runnable Vite + React app with tokens, Tailwind and a template screen wired
  (`data-app`, `ai-assistant`, `flow-workspace`, `settings`, `marketing` too).
- **Add it to an app you have:** [Getting Started](?path=/docs/docs-getting-started--docs) —
  `pnpm add`, two lines of CSS, one provider.
- **Point your coding agent at it, nothing to install:**
  `claude mcp add --transport http brand-ui https://elabs-ai.com/mcp` — see
  [brand-ui MCP Server](?path=/docs/docs-brand-ui-mcp-server--docs). Every component
  page below carries the same intent block the agent reads.
- **Choose the right component:** [Choosing between similar components](?path=/docs/docs-choosing-between-similar-components--docs).

## Let the agent design the screen

When a screen cannot be written ahead of time — the order the user just asked about, a
form for what the agent still needs — the agent emits it as **data**: a tree of catalog
types that brand-ui validates and renders with the real components, actions routed back
to your app. See [Generative UI (A2UI)](?path=/docs/docs-generative-ui-a2ui--docs).

## See it whole

Every template below is a real, full-screen story — switch **Theme** and **Mode**
in the toolbar and it re-renders; open it in its own tab with the corner icon.

<Canvas of={DashboardTemplate.Default} story={{ inline: false, height: "560px" }} />

<Canvas of={AiAssistantTemplate.Default} story={{ inline: false, height: "560px" }} />

Those two are **starters** — the archetypes `brand-ui create` scaffolds. The rest of the
starters: [Data App](?path=/story/patterns-templates-starters-data-app--default),
[Flow Workspace](?path=/story/patterns-templates-starters-flow-workspace--default) and
[Settings](?path=/story/patterns-templates-starters-settings--default).

The **use-case templates** are whole products built from the registry's blocks, grouped by
who builds them:
[Revenue Operations](?path=/story/patterns-templates-analytics-revenue-operations--default),
[Logistics Control Tower](?path=/story/patterns-templates-operations-logistics-control-tower--default),
[Incident Command](?path=/story/patterns-templates-operations-incident-command--default),
[Process Explorer](?path=/story/patterns-templates-operations-process-explorer--default),
[Customer 360](?path=/story/patterns-templates-customers-customer-360--default),
[Support Desk](?path=/story/patterns-templates-customers-support-desk--default),
[Project Hub](?path=/story/patterns-templates-product-teams-project-hub--default) and the
[Agent Operations Center](?path=/story/patterns-templates-ai-products-agent-operations-center--default).

## Source and packages

- **Source:** [github.com/mreimitz/elabs-components](https://github.com/mreimitz/elabs-components)
  · **npm:** [`@elabs-ai` on npm](https://www.npmjs.com/org/elabs-ai) — every package below is public
- **Full guide:** [README](https://github.com/mreimitz/elabs-components#readme) ·
  [Consuming guide](https://github.com/mreimitz/elabs-components/blob/main/docs/CONSUMING.md) ·
  [Registry guidelines](https://github.com/mreimitz/elabs-components/blob/main/docs/REGISTRY_GUIDELINES.md)

## Packages

{/* This list is GENERATED from the manifest by `pnpm gen` (PKG_PURPOSE in packages/cli/lib/render-docs.mjs). Do not hand-edit between the markers. */}

{/* brand-ui:gen:packages:start */}

- **@elabs-ai/components-tokens** — Semantic CSS-variable themes + ThemeProvider/useTheme.
- **@elabs-ai/components-icons** — Brand/product-vocabulary icons + BrandLogo (generic glyphs use lucide-react).
- **@elabs-ai/components-ui** — Foundation + app UI (Button, Card, Dialog, Tabs, AppShell, …).
- **@elabs-ai/components-data** — TanStack DataTable, FilterBar, SearchInput, FacetFilter, ColumnPicker.
- **@elabs-ai/components-ai** — ChatShell, Conversation, Message, PromptInput, Tool, Reasoning, citations.
- **@elabs-ai/components-flow** — Branded React Flow canvas, nodes, edges, controls, inspector.
- **@elabs-ai/components-maps** — MapLibre GL maps: MapCanvas, markers, popups, controls, routes, arcs, GeoJSON, clusters.
- **@elabs-ai/components-charts** — MetricCard, MetricGrid, ChartCard, ChartFrame (expand/flip/download).
- **@elabs-ai/components-marketing** — Hero, FeatureGrid, UseCaseCard, StatsBand, CTASection, LogoStrip.
- **@elabs-ai/components-editor** — Token-themed Monaco editor: CodeEditor, DiffEditor, CodeWorkspace.
- **@elabs-ai/components-viewer** — FileViewer — any file (image, text, JSON, CSV) via a pluggable adapter registry.
- **@elabs-ai/components-terminal** — Terminal surfaces: shell/agent output and coding-agent CLI look-alikes.
- **@elabs-ai/components-process** — Process mining and event-log analysis: process map, variants, cases, conformance — composes flow/charts/data.
  {/* brand-ui:gen:packages:end */}

## Theming

Pick a theme family with the **Theme** toolbar control (Default, plus the
downloadable families such as Ocean, Qlik and Snowflake) and flip **Mode**
between light and dark. Themes are driven entirely by `data-theme` +
semantic tokens defined in `@elabs-ai/components-tokens/themes.css`. Components never hardcode
colors — they reference tokens like `bg-background`, `text-muted-foreground`,
`border-border`, so re-branding (e.g. swapping the primary hue) is a token change.

## Two ways to consume

### 1. Import stable primitives

```sh
pnpm add @elabs-ai/components-tokens @elabs-ai/components-ui
```

```tsx
import { Button } from "@elabs-ai/components-ui";
```

Each package links from [npm](https://www.npmjs.com/org/elabs-ai); for example
[`@elabs-ai/components-ui`](https://www.npmjs.com/package/@elabs-ai/components-ui).

### 2. Copy-own prototype blocks from the registry

Blocks and templates live in the brand-ui shadcn-compatible registry
([source](https://github.com/mreimitz/elabs-components/tree/main/registry)). Install
one by its full URL:

```sh
npx shadcn@latest add https://elabs-ai.com/r/data-table.json
```

Or register the `@brand-ui` namespace once in your app's `components.json` and
install by name:

```json
{
  "registries": {
    "@brand-ui": "https://elabs-ai.com/r/{name}.json"
  }
}
```

```sh
npx shadcn@latest add @brand-ui/data-table
```

Swap `latest` for a release version (e.g. `4.2.0`) to pin a block.
