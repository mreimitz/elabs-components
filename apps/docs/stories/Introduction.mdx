import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Docs/Introduction" />

# brand-ui

A **source-owned, token-driven** React component system for internal apps,
prototypes, POCs, AI/chat clients, data grids, dashboards and React Flow
surfaces. The default look is modern enterprise SaaS — restrained, app-first,
and themeable to any brand.

- **Source:** [github.com/mreimitz/elabs-components](https://github.com/mreimitz/elabs-components)
- **npm:** [`@elabs-ai` on npm](https://www.npmjs.com/org/elabs-ai) — every package below is public
- **Full guide:** [README](https://github.com/mreimitz/elabs-components#readme) ·
  [Consuming guide](https://github.com/mreimitz/elabs-components/blob/main/docs/CONSUMING.md) ·
  [Registry guidelines](https://github.com/mreimitz/elabs-components/blob/main/docs/REGISTRY_GUIDELINES.md)

## Packages

{/* This list is GENERATED from the manifest by `pnpm gen` (PKG_PURPOSE in packages/cli/lib/render-docs.mjs). Do not hand-edit between the markers. */}

{/* brand-ui:gen:packages:start */}

- **@elabs-ai/components-tokens** — Semantic CSS-variable themes + ThemeProvider/useTheme.
- **@elabs-ai/components-icons** — Brand/product-vocabulary icons + BrandLogo (generic glyphs use lucide-react).
- **@elabs-ai/components-ui** — Foundation + app UI (Button, Card, Dialog, Tabs, AppShell, …).
- **@elabs-ai/components-data** — TanStack DataTable, FilterBar, SearchInput, FacetFilter, ColumnPicker.
- **@elabs-ai/components-ai** — ChatShell, Conversation, Message, PromptInput, Tool, Reasoning, citations.
- **@elabs-ai/components-flow** — Branded React Flow canvas, nodes, edges, controls, inspector.
- **@elabs-ai/components-maps** — MapLibre GL maps: MapCanvas, markers, popups, controls, routes, arcs, GeoJSON, clusters.
- **@elabs-ai/components-charts** — MetricCard, MetricGrid, ChartCard, ChartFrame (expand/flip/download).
- **@elabs-ai/components-marketing** — Hero, FeatureGrid, UseCaseCard, StatsBand, CTASection, LogoStrip.
- **@elabs-ai/components-editor** — Token-themed Monaco editor: CodeEditor, DiffEditor, CodeWorkspace.
- **@elabs-ai/components-viewer** — FileViewer — any file (image, text, JSON, CSV) via a pluggable adapter registry.
- **@elabs-ai/components-terminal** — Terminal surfaces: shell/agent output and coding-agent CLI look-alikes.
- **@elabs-ai/components-process** — Process mining and event-log analysis: process map, variants, cases, conformance — composes flow/charts/data.
  {/* brand-ui:gen:packages:end */}

## Theming

Switch themes with the toolbar (sun/paint icon): **Light** (default) and
**Dark**. Themes are driven entirely by `data-theme` +
semantic tokens defined in `@elabs-ai/components-tokens/themes.css`. Components never hardcode
colors — they reference tokens like `bg-background`, `text-muted-foreground`,
`border-border`, so re-branding (e.g. swapping the primary hue) is a token change.

## Two ways to consume

### 1. Import stable primitives

```sh
pnpm add @elabs-ai/components-tokens @elabs-ai/components-ui
```

```tsx
import { Button } from "@elabs-ai/components-ui";
```

Each package links from [npm](https://www.npmjs.com/org/elabs-ai); for example
[`@elabs-ai/components-ui`](https://www.npmjs.com/package/@elabs-ai/components-ui).

### 2. Copy-own prototype blocks from the registry

Blocks and templates live in the brand-ui shadcn-compatible registry
([source](https://github.com/mreimitz/elabs-components/tree/main/registry)). Install
one by its full URL:

```sh
npx shadcn@latest add https://mreimitz.github.io/elabs-components/r/latest/data-table.json
```

Or register the `@brand-ui` namespace once in your app's `components.json` and
install by name:

```json
{
  "registries": {
    "@brand-ui": "https://mreimitz.github.io/elabs-components/r/latest/{name}.json"
  }
}
```

```sh
npx shadcn@latest add @brand-ui/data-table
```

Swap `latest` for a release version (e.g. `4.2.0`) to pin a block.
