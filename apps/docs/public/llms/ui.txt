# @elabs-ai/components-ui

> Foundation + app UI (Button, Card, Dialog, Tabs, AppShell, …).

Import from `@elabs-ai/components-ui`. Part of brand-ui — see the root `llms.txt` for routing.

## Components

- Accordion
- AccordionContent
- AccordionItem
- AccordionTrigger
- AdvancedGroup
- Alert (variant=default*|info|success|warning|destructive)
- AlertDescription
- AlertDialog — Confirmation overlay for destructive / irreversible actions — friction proportional to consequence.
  - avoid: Firing the destructive action immediately on click — require the AlertDialog confirm (or an undo window).
  - avoid: Making the destructive action the default-focused button — focus the Cancel/safe action first.
- AlertDialogAction
- AlertDialogCancel
- AlertDialogContent
- AlertDialogDescription
- AlertDialogFooter
- AlertDialogHeader
- AlertDialogPortal
- AlertDialogTitle
- AlertDialogTrigger
- AlertTitle
- APPROVAL_SCOPE_DESCRIPTION_KEYS
- AppShell — Top-level application frame — sidebar + header + content region.
  - avoid: Deep wrapper nesting inside the shell — keep the DOM shallow; one element, one job.
  - avoid: Painting the shell's chrome recess in the component — the chrome→canvas hierarchy is a THEME property; fix --sidebar's lightness, not AppShell.
- AppSidebar — The opinionated application sidebar: the Sidebar skeleton behind typed header and footer slots, with the navigation as children.
  - avoid: Reaching for it when the shell is genuinely bespoke — then compose the Sidebar primitive set directly instead of fighting the typed slots.
  - avoid: Passing a whole nav tree through the header slot — header is the identity row; the navigation is children.
  - avoid: Forgetting SidebarInset for the content pane — the canvas must read brighter than the chrome, which is what stops an app shell going flat.
- AspectRatio
- AttributionPanel
- ATTRIBUTIONS
- Avatar
- AvatarFallback
- AvatarImage
- Badge (variant=default*|secondary|outline|success|warning|destructive|info) — Compact status/label chip (status, count, category).
  - avoid: Using it to carry an execution status — that is StatusBadge, whose closed seven-state vocabulary pairs a distinct glyph with every tone so the state survives greyscale.
  - avoid: Using a Badge as a clickable control — use a Button (Badge is not interactive).
  - avoid: Encoding status by color alone — pair the color with text/icon for non-color users.
- BentoGrid
- BentoGridItem (hero=true|false* · interactive=true|false*)
- BoundedNumber
- Breadcrumb
- BreadcrumbEllipsis
- BreadcrumbItem
- BreadcrumbLink
- BreadcrumbList
- BreadcrumbPage
- BreadcrumbSeparator
- Button (variant=default*|secondary|destructive|outline|outline-subtle|ghost|link · size=sm|default*|lg|icon|icon-sm|icon-lg) — Primary action trigger — the canonical way to invoke an action.
  - avoid: Two primary Buttons in the same action group — demote one to secondary/outline.
  - avoid: Button used for navigation — use a link instead (asChild + <a>, or the variant="link").
  - avoid: destructive variant for an irreversible action with no confirm step — wrap in AlertDialog.
  - avoid: Icon-only Button without an aria-label — the control has no accessible name.
- ButtonGroup (orientation=horizontal*|vertical)
- ButtonGroupSeparator
- ButtonGroupText
- Calendar
- Card (interactive=true|false*) — Surface grouping related content into a bordered, padded block.
  - avoid: Card inside a Card (nested cards) — flatten; cards are not a generic wrapper.
  - avoid: Card-for-everything — use a Card only when a bordered surface is the right affordance.
  - avoid: Identical icon-tile-above-heading Card grids repeated endlessly — vary structure.
- CardAction
- CardContent
- CardDescription
- CardFooter
- CardHeader
- CardTitle
- Carousel
- CarouselContent
- CarouselItem
- CarouselNext
- CarouselPrevious
- CATEGORY_LABEL
- ChangeReview
- ChangeReviewHeader
- ChangeReviewHunk
- ChangeReviewList
- ChangeReviewProvenance
- ChangeReviewProvider
- Checkbox — Binary on/off toggle within a form (multi-select within a group).
  - avoid: Checkbox and its Label not sharing one hit target — clicking the label must toggle the box (no dead zone).
  - avoid: Using a Checkbox for mutually-exclusive choices — use RadioGroup.
- Collapsible
- CollapsibleContent
- CollapsibleTrigger
- COLOR_TOKENS
- ColorPicker
- Combobox — Searchable single/multi select — Select with typeahead over a large or async option set.
  - avoid: Using Combobox for a tiny fixed option set — a plain Select is simpler and lighter.
  - avoid: Restyling the trigger/panel by hand — Combobox is a Popover + Command assembly; style through those parts.
- Command
- CommandDialog
- CommandEmpty
- CommandGroup
- CommandInput
- CommandItem
- CommandList
- CommandSeparator
- CommandShortcut
- ConfirmDialog
- ContextMenu
- ContextMenuCheckboxItem
- ContextMenuContent
- ContextMenuGroup
- ContextMenuItem
- ContextMenuLabel
- ContextMenuRadioGroup
- ContextMenuRadioItem
- ContextMenuSeparator
- ContextMenuShortcut
- ContextMenuSub
- ContextMenuSubContent
- ContextMenuSubTrigger
- ContextMenuTrigger
- COPY_FEEDBACK_MS
- CopyableValue
- DatePicker
- DateRangePicker
- DEFAULT_MESSAGES
- Descriptions
- DescriptionsItem
- Dialog — Modal overlay for focused tasks/flows that block the page until dismissed.
  - avoid: Dialog without a DialogTitle — screen readers announce no name (use sr-only if visually hidden).
  - avoid: Nesting a Dialog inside a Dialog — use a single flow or a Sheet/Drawer for the secondary surface.
  - avoid: Confirming a destructive action in a plain Dialog — use AlertDialog (it traps focus on the safe action).
- DialogBody
- DialogClose
- DialogContent (size=sm|lg*|xl|full)
- DialogDescription
- DialogFooter
- DialogHeader
- DialogOverlay
- DialogPortal
- DialogSection
- DialogTitle
- DialogTrigger
- DOCUMENT_ADDRESS_KINDS
- Drawer
- DrawerClose
- DrawerContent
- DrawerDescription
- DrawerFooter
- DrawerHeader
- DrawerPortal
- DrawerTitle
- DrawerTrigger
- DropdownMenu
- DropdownMenuCheckboxItem
- DropdownMenuContent
- DropdownMenuGroup
- DropdownMenuItem
- DropdownMenuLabel
- DropdownMenuPortal
- DropdownMenuRadioGroup
- DropdownMenuRadioItem
- DropdownMenuSeparator
- DropdownMenuShortcut
- DropdownMenuSub
- DropdownMenuSubContent
- DropdownMenuSubTrigger
- DropdownMenuTrigger
- EmptyListIllustration
- EmptyState
- ErrorIllustration
- ErrorState
- ExpandDialog
- ExpandDialogContent
- ExpandDialogHeader
- ExpandDialogPanes (detailPlacement=side*|bottom · stackBelow=never*|sm|md|lg)
- FieldControl
- FieldDescription
- FieldError
- FieldLabel
- FieldRoot
- FieldRow
- FILE_CATEGORY_ICONS
- FileUpload
- FileUploadDropzone
- FileUploadItem
- FileUploadList
- FilterChip
- FirstRunIllustration
- Form — Validated form scaffold (Field/Label/Control/Message) wiring inputs to a schema.
  - avoid: Disabling submit until valid — keep submit enabled until the request starts, then show a spinner.
  - avoid: Errors not rendered inline next to the field; not focusing the first error on submit.
  - avoid: Warning the user before navigating away from unsaved changes is missing.
- FormControl
- FormDescription
- FormField
- FormItem
- FormLabel
- FormMessage
- Heading (size=display|title*|subtitle)
- HoverCard
- HoverCardContent
- HoverCardTrigger
- IconButton
- ILLUSTRATION_ACCENT_VAR
- Input — Single-line text field — the base form input.
  - avoid: Hand-building a search field on it — a leading icon plus a clear button is SearchInput in @elabs-ai/components-data.
  - avoid: Input without an associated Label (visible or sr-only) — it has no accessible name.
  - avoid: Blocking paste (onPaste + preventDefault) — never block paste.
  - avoid: Wrong type/inputmode — use type=email/tel/url/number + inputmode so mobile keyboards + validation match.
  - avoid: Missing autocomplete + a meaningful name on a real form field.
- InputGroup (variant=outline*|surface|card)
- InputGroupAddon (align=inline-start*|inline-end|block-start|block-end)
- InputGroupButton
- InputGroupInput
- InputGroupText
- InputGroupTextarea
- InputOTP
- InputOTPGroup
- InputOTPSeparator
- InputOTPSlot
- Kbd
- KeyboardShortcuts — Grouped, searchable presentation of an application's shortcut set — the sheet the Kbd atom belongs in.
  - avoid: Rendering a raw key string instead of Kbd — the atom carries the platform glyphs and the translate opt-out.
  - avoid: Showing a group's count without its items — a count with nothing behind it is a claim the sheet cannot keep.
  - avoid: Leaving an empty filter result blank — an empty search renders a real empty state, not a blank region.
- KeyValueEditor
- Label
- LinkPreview
- LinkPreviewCard
- ListEditor
- LoadingState
- LocaleProvider
- MatchHighlight
- MentionInput
- MentionInputContent
- MentionInputEmpty
- MentionInputItem
- MentionInputList
- MentionInputTextarea
- Menubar
- MenubarCheckboxItem
- MenubarContent
- MenubarGroup
- MenubarItem
- MenubarMenu
- MenubarPortal
- MenubarRadioGroup
- MenubarRadioItem
- MenubarSeparator
- MenubarShortcut
- MenubarSub
- MenubarSubContent
- MenubarSubTrigger
- MenubarTrigger
- MetricCard — Single KPI tile — label, value, delta/trend.
  - avoid: Numbers without tabular-nums — figures jitter across a row; use tabular-nums for any number column.
  - avoid: Encoding up/down by color alone — pair with an arrow/sign.
  - avoid: Re-declaring a KPI tile in another package — MetricCard is owned by @elabs-ai/components-ui; charts and editor re-export it (ADR 0012).
- ModelPicker
- NavigationMenu
- NavigationMenuContent
- NavigationMenuItem
- NavigationMenuLink
- NavigationMenuList
- NavigationMenuTrigger
- NavigationMenuViewport
- NavMain
- NavNotifications
- NavUser
- NoAccessIllustration
- NoResultsIllustration
- NumberInput
- OfflineIllustration
- PageShell
- Pagination
- PaginationContent
- PaginationEllipsis
- PaginationItem
- PaginationLink
- PaginationNext
- PaginationPrevious
- Popover — Anchored, dismissible floating panel for lightweight contextual content.
  - avoid: Reimplementing dismissal/positioning by hand — let the Radix Popover own focus + outside-click.
  - avoid: Expecting PopoverContent to render its own focus ring — it is outline-none by design; the trigger carries the ring.
- PopoverAnchor
- PopoverContent
- PopoverTrigger
- Progress
- PROSE_HEADING_REM
- PROSE_HEADING_TRACKING
- PROSE_HEADING_WEIGHT
- ProseBlockquote
- ProseHeading
- ProseInlineCode
- ProseLink
- ProseList
- ProseListItem
- ProseText
- RadioGroup — Mutually-exclusive single choice from a small visible set.
  - avoid: Using a RadioGroup for many options — use Select/Combobox past ~7 options.
  - avoid: RadioGroup items without labels sharing a hit target.
- RadioGroupItem
- Rating
- ResizableHandle
- ResizablePanel
- ResizablePanelGroup
- ResultCount
- Reveal (appear=fade|up*|down|left|right|zoom · speed=fast|base|slow*)
- RevealGroup
- RevisionTimeline (density=comfortable*|compact)
- SchemaForm
- SchemaFormDescription
- SchemaFormError
- SchemaFormFallback
- SchemaFormField
- SchemaFormFields
- SchemaFormProvider
- SchemaFormRoot
- SchemaFormSubmit
- SchemaFormTestAction
- SchemaFormTitle
- ScrollArea
- ScrollBar
- SectionHeader
- SegmentedField
- Select — Single-choice dropdown from a known set of options.
  - avoid: Using Select for free-form or searchable values — use Combobox when the user may type/search.
  - avoid: Select without a Label — no accessible name.
- SelectContent
- SelectGroup
- SelectItem
- SelectLabel
- SelectSeparator
- SelectTrigger
- SelectValue
- Separator
- Sheet — Edge-anchored panel (left/right/top/bottom) for secondary flows beside the page.
  - avoid: Sheet without a SheetTitle — no accessible name.
  - avoid: Scroll chaining to the page behind it — set overscroll-behavior: contain.
- SheetClose
- SheetContent
- SheetDescription
- SheetFooter
- SheetHeader
- SheetPortal
- SheetTitle
- SheetTrigger
- Sidebar — The sidebar PRIMITIVE set you assemble yourself — provider, rail, header/content/footer regions and the menu parts.
  - avoid: Rebuilding the Sidebar -> SidebarHeader -> SidebarContent -> SidebarFooter skeleton by hand — that skeleton IS AppSidebar, which takes typed header/footer slots.
  - avoid: Rendering it outside SidebarProvider — the open state, the keyboard shortcut and the cookie persistence all live on the provider.
  - avoid: Conditionally mounting it to collapse it — the collapse is a width tween on an always-mounted rail; a conditional mount cannot animate.
- SidebarContent
- SidebarFooter
- SidebarGroup
- SidebarGroupAction
- SidebarGroupContent
- SidebarGroupLabel
- SidebarHeader
- SidebarInput
- SidebarInset
- SidebarMenu
- SidebarMenuAction
- SidebarMenuBadge
- SidebarMenuButton (variant=default*|outline · size=default*|sm|lg)
- SidebarMenuItem
- SidebarMenuSkeleton
- SidebarMenuSub
- SidebarMenuSubButton
- SidebarMenuSubItem
- SidebarProvider
- SidebarRail
- SidebarSeparator
- SidebarTrigger
- Skeleton
- Slider
- SliderNumber
- Spinner
- SplitPanel
- StatePanel (kind=empty*|error|loading)
- STATUS_LABELS
- STATUS_ROLE
- STATUS_TONE_ICONS
- STATUS_TONES
- StatusBadge (status=pending|running|complete|awaiting-approval|denied|failed|skipped · tone=neutral|info|success|warning|destructive · size=sm|md*) — The closed seven-state execution-status vocabulary, each state carrying its own icon as well as its own tone.
  - avoid: Using it for a neutral label or a count — that is Badge; this one asserts an execution state.
  - avoid: Inventing an eighth status by passing a custom label into the canonical enum — map an out-of-vocabulary state ONCE, near your domain, through the {label, tone, icon} form.
  - avoid: Relying on the tone alone to distinguish two states — the glyph is the second channel that keeps them apart in greyscale.
- STATUSES
- StatusIcon
- STREAMDOWN_TRANSLATION_KEYS
- SuccessIllustration
- Switch — Immediate on/off setting toggle (applies on change, not on submit).
  - avoid: Switch that needs a separate Save to apply — use a Checkbox in a form instead; a Switch should apply immediately.
- Table
- TableBody
- TableCaption
- TableCell
- TableFooter
- TableHead
- TableHeader
- TableRow
- Tabs — Switch between peer views in the same context without navigating away.
  - avoid: Tabs for sequential steps — use a stepper/wizard; tabs are for peer, non-sequential views.
  - avoid: More than ~7 tabs — consider a Select or a sidebar nav.
- TabsContent
- TabsList
- TabsTrigger
- TagInput
- TeamSwitcher
- Text (variant=lead|body*|caption|meta|kpi|code · tone=default*|muted|primary)
- TEXT_ROLE_REM
- Textarea
- ThemeSwitcher
- Timeline
- TimelineItem
- TimelineRoot
- Toaster
- Toggle (variant=default*|outline|segmented · size=default*|sm|lg)
- ToggleGroup
- ToggleGroupItem
- Toolbar (orientation=horizontal*|vertical) — A dense row of controls that acts on nearby content, collapsed into ONE tab stop with arrow-key navigation between the controls.
  - avoid: Using it for the ordinary control row above a list or table — that is ViewToolbar, where every control is its own tab stop.
  - avoid: Omitting aria-label — a toolbar is one tab stop, so an unnamed one announces nothing about what it acts on.
  - avoid: Putting a text input or a link inside it — the roving tabindex swallows the arrow keys those controls need.
  - avoid: Reaching for it to attach actions to a canvas node — that is NodeToolbar in @elabs-ai/components-ai.
- ToolbarButton
- ToolbarSeparator
- ToolbarSlot
- ToolbarToggleGroup
- ToolbarToggleItem
- Tooltip — Transient hover/focus hint with supplementary (non-essential) text.
  - avoid: Putting essential information ONLY in a Tooltip — it is unreachable on touch and transient; keep critical content visible.
  - avoid: Tooltip on a non-focusable element — it won't show on keyboard focus.
- TooltipContent
- TooltipProvider
- TooltipTrigger
- TopNav
- Transfer
- Tree
- TreeSelect
- UnsupportedJsonSchemaError
- ViewToolbar — The row above a list, table or board — status and filters on the left, actions on the right, every control its own tab stop.
  - avoid: Giving it role=toolbar — it deliberately does not claim that role; a dense row that should cost ONE tab stop with arrow-key roving is Toolbar.
  - avoid: Putting the primary page action in it — this row acts on the VIEW; a page-level action belongs in the page header.
  - avoid: Hiding the result count while filters are applied — the count is how a person tells an empty filter from an empty dataset.
- ViewToolbarFilters
- VirtualSelect
- Wizard
- WizardNav
- WizardStep
- WizardSteps
- WorkspacePicker — Choose the workspace or project directory a session runs against, from recents or a typed path.
  - avoid: Re-implementing the picker list — WorkspacePicker composes ModelPicker; a second searchable-popover list is the duplication this component exists to avoid.
  - avoid: Marking the current workspace with a glyph alone — the marker is a word in the accessible name (1.4.1).
  - avoid: Reading the filesystem — the component takes a workspace list as a prop and never enumerates directories itself.
  - avoid: Natively disabling the path submit — an empty field uses aria-disabled plus a handler guard, so focus is never stranded.

## Hooks

- useCollapsiblePanel
- useCommandActiveItemId
- useCopyToClipboard
- useDialogDismissGuard
- useDiffRows
- useFileUpload
- useFormField
- useIsMobile
- useLocale
- useMentionInput
- useSidebar
- useStreamdownTranslations
- useThemeTransition
- useTreeKeyboard
- useVirtualListbox
