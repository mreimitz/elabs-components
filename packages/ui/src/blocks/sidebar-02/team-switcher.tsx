/**
 * sidebar-02 — team switcher.
 * Re-exports the shared `TeamSwitcher` primitive from `@elabs/components-ui` so this block
 * no longer maintains its own copy (issue #99).
 */
export { TeamSwitcher } from "@elabs/components-ui";
export type { TeamSwitcherProps, TeamSwitcherTeam } from "@elabs/components-ui";
