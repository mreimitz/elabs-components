"use client";

// `Video` is `Media` under the name a picture-first caller reaches for — one
// player for any file (a sound-only file draws its waveform). See `media.tsx`.
export {
  Media as Video,
  type MediaProps as VideoProps,
  type MediaTrack as VideoTrack,
} from "./media";
