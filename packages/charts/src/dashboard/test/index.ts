/**
 * `@elabs-ai/components-charts/dashboard/test` — the engine-free dashboard test double
 * (ADR 0037 §2). Placeholder so the subpath resolves and builds; RM-077 fills it
 * (`assertDashboardSpec`, `DashboardSpecError`, a `DashboardSheet` double). Never import
 * zustand, `@dnd-kit/*`, visx/d3/motion or a chart barrel at runtime from here.
 */

// Spec test double — RM-077

export {};
