---
"@elabs-ai/components-viewer": minor
---

FileViewer's image, docx and media adapters render through ui `Image`, `Audio` and `Video`: token-styled player controls with keyboard shortcuts replace the browser's native chrome.
