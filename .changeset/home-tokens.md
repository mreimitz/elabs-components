---
"@elabs-ai/components-marketing": patch
---

`GatesBand`'s own heading now steps down to `text-subtitle` instead of matching a hosting section's `text-title` h2 one-for-one, which read as a flat hierarchy on the home page's "One token system" band.

`GatesBand`'s group label now uses the `text-eyebrow` role instead of a hand-rolled `text-caption font-medium uppercase` combination.
