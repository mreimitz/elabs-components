---
"@elabs-ai/components-tokens": patch
---

Light theme: `--sidebar-muted-foreground` is a touch lighter (0.72 → 0.745 L) so a hovered sidebar row's muted meta line (the `NavUser` email) clears WCAG AA on `--sidebar-accent` (4.36:1 → ≥4.5:1).
