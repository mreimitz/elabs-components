---
"@elabs-ai/components-data": patch
---

DataGrid: fit-to-width and auto-size no longer count a table nested in a master / detail row, so an expanded detail table can't crush the parent grid's columns.
