node_modules
dist
storybook-static
coverage
pnpm-lock.yaml
registry/__output
.turbo
*.min.*
# Generated artifact — the brand-ui CLI is its single source of truth (WP-10 #85).
# Excluded so the stale-gate diffs the generator's exact output, not Prettier's.
brand-ui.manifest.json
# Generated agent docs (WP-03 #87/#156/#82) — same single-source rule: their
# stale-gates (`pnpm inventory:check` / `llms:check` / `context:check`) diff the
# generator's exact output, so Prettier must not reformat them.
apps/docs/public/component-inventory.md
apps/docs/public/brand-ui-context.md
apps/docs/public/llms.txt
apps/docs/public/llms/
# Review evidence (#288-#307) — the six contact sheets are captured browser output
# accompanying 166 screenshots. They are artifacts of a review run, not source, and
# their value is being exactly what was rendered, so Prettier must not rewrite them.
apps/e2e/reports/screenshots/
